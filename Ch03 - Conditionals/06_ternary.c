#include<stdio.h>

int main(){
    int a = 15;
    int b = 18;

    a>b ? printf("a is greater than b") : printf("b is greater than a");
    return 0;
}

// format:
// condition ? (if-true) : (if-not true);
